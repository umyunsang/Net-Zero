# Progress: Net-Zero consumer product UI redesign

## 2026-08-13
- Routed the feedback as a material product-design revision, not a small styling fix.
- Read the active planning, brainstorming, and frontend design procedures completely.
- Paused the prior BM-loop UI direction; preserved its functional work and runtime evidence.
- Started bounded discovery using only the supplied PPT, current project authority, and current visible UI before seeking `SK@v1` approval.
- No product code has been changed for this redesign.
- Completed a compact memory pass and treated it only as navigation to durable business contracts, not current design authority.
- Confirmed the supplied PPT can be inspected read-only with the bundled workspace runtime.
- Rendered all seven image-based slides to temporary PNGs for visual inspection; no project or source-PPT content was altered.
- Inspected the deck overview and key slides. Extracted only product promise, three activity nouns, reward/community value, trust principles, and a restrained green/orange visual cue set; explicitly rejected its editorial typography and presentation structures for the product UI.
- Reconnected to the existing local in-app browser tab for read-only current-state inspection.
- The first tab-attachment call used an obsolete `claim` method and failed without changing browser state; switching to the current tab API.
- Confirmed the browser session is connected; the remaining step is attaching to the listed ambient tab with the current API.
- Attached to the existing local product tab with the current browser API; ready for read-only DOM and screenshot inspection.
- Inspected the live Rewards DOM and captured the rejected desktop surface. Confirmed the core problem is information architecture and disclosure hierarchy, not missing business functionality.
- Inspected the frontend component map and internal-copy usage. Confirmed the consumer, reviewer, merchant, and admin concerns are co-located in one large app file and that user navigation mirrors system modules.
- Inspected the live Home DOM after data load. Confirmed it combines pitch, audit dashboard, internal verification mechanics, privacy policy, and transaction tutorial into one end-user page.
- The current browser did not transition after two exact navigation clicks to the activity screen. Discovery is continuing from the current source without mutating data; interaction will be re-tested after redesign implementation.
- Completed bounded PPT/current-UI discovery and compared three information-architecture directions.
- Created `SK@v1` with the recommended consumer-wallet product model, explicit consumer/operator disclosure boundary, complete screen inventory, 0-point-to-voucher flow, responsive direction, locked functional semantics, and approval scope.
- Set workflow state to `SKELETON_APPROVAL_PENDING(SK@v1)`; no product code changed.
- Recorded the stable skeleton fingerprint `e655a489249628ec64c1b5a0947df01270122ee0e50e05d891a5696b32dcc8ca` and advanced the active plan to Phase 2.
- Recorded the user's exact `SK@v1` approval receipt and verified the fingerprint is unchanged.
- Marked the research manifest empty because no external/deep-research question remains; started bounded visual synthesis toward `DS@v1`.
- Confirmed concept generation is authorized while product-code changes remain prohibited pending exact `DS@v1` approval.
- The initial Home concept call failed before generation because an optional image-history field was set to zero; corrected by omitting it for a brand-new image.
- Generated the first full mobile Home concept at the 0-point state. It establishes the product-like white/green/orange system and approved four-job navigation without presentation/internal-document UI.
- Reviewed the generated preview and recorded minor density/avatar refinements. The first path-based `view_image(original)` rendering cropped unexpectedly, so an inspection-friendly derivative is required before design approval.
- Created and inspected the Home derivative successfully with `view_image(high)`.
- Generated and inspected the coordinated mobile Activity hub. It separates the three tasks cleanly and preserves the consumer disclosure boundary without presentation-style narration.
- Generated and inspected the coordinated Tree capture screen. It proves the activity forms can become focused consumer tasks with contextual disclosure rather than three stacked internal/demo forms.
- Generated and inspected the 23-point Wallet screen. It shows reward affordability and the absence of vouchers entirely through live consumer state, satisfying the BM visibility goal without a tutorial panel.
- Generated and inspected the active Voucher detail after a 20-point exchange, showing a 3-point balance and a one-use QR/code handoff without exposing merchant or transaction internals.
- Generated and inspected the coordinated desktop Home. The product hierarchy survives the wider viewport without reverting to a pitch deck or internal dashboard.
- Completed the primary concept set: mobile Home, Activity hub, Tree capture, Wallet, Voucher detail, and desktop Home.
- Generated and inspected the desktop Wallet, completing the approved responsive Home/Wallet concept scope.
- Synthesized the inspected concept set into `DS@v1`, including exact Thai copy, tokens, component families, responsive rules, consumer/internal boundary, interaction states, architecture split, and acceptance criteria.
- Set workflow state to `SYNTHESIZED_DESIGN_PENDING_APPROVAL(DS@v1)`; product code remains unchanged.
- Self-reviewed `DS@v1` for placeholders, contradictions, scope, ambiguity, concept-file presence, and business-state consistency; no material issue remains.
- Recorded design SHA-256 `6607e306dcb68870a5e892b69073216342a8f979e78ca47aeeabe2c78e478f13`.
- Review-sheet QA note: loading the mobile and desktop contact sheets simultaneously exceeded the output context. Retrying one sheet per `view_image` call; this does not affect the already verified standalone concepts.
- Inspected the mobile and desktop review sheets separately. Responsive hierarchy, business-state continuity, and the consumer/internal disclosure boundary are visually consistent across the complete concept set.
- Reverified the unchanged 240-line `DS@v1` fingerprint as `6607e306dcb68870a5e892b69073216342a8f979e78ca47aeeabe2c78e478f13`. Git working-tree verification is unavailable because the project directory is not a Git repository; this turn's writes remain confined to planning/review artifacts.
- Recorded the user's exact approval `DS@v1 승인` against the unchanged design fingerprint. The workflow is now `DESIGN_APPROVED_FOR_PLANNING(DS@v1)` and Phase 3 implementation planning is active.
- Inspected the React/Vite frontend layout, package scripts, component outline, and existing E2E coverage. Confirmed the redesign can preserve backend contracts while replacing the consumer shell and retaining separate operator views.
- Completed the implementation discovery pass for activity submissions, wallet state, consumer/operator tests, and the current stylesheet. No backend endpoint or scoring/voucher semantic change is required.
- Wrote `implementation_plan_v1.md` with file ownership, preserved contracts, execution order, and rendered acceptance gates. Set workflow state to `IMPLEMENTATION_AUTHORIZED(DS@v1)`; implementation remains limited to the local frontend and its tests.
- Added shared product types, token-aware API/evidence helpers, semantic UI primitives, and a consistent local SVG icon family.
- Implemented the consumer-first Home, Activity hub and focused capture, History, Wallet, code-derived QR voucher detail, Profile/community, and mobile/desktop shell.
- Isolated reviewer, merchant, and admin workflows in `OperationsApp`; reduced `App.tsx` to consumer entry plus session/role composition.
- Replaced the presentation/dashboard stylesheet with the approved true-white token system, mobile bottom navigation, desktop rail, focused forms, reward/voucher states, and separate utility workspace styles.
- Added local `qrcode` generation plus its TypeScript definitions; no runtime CDN or remote visual asset is used.
- Focused web TypeScript check passes after the component split.
- Updated Playwright coverage to the approved consumer navigation, contextual 0-point state, 23→3 exchange, real QR voucher, terminal used state, consumer-safe history mapping, Profile consent, and unchanged operator contracts.
- Fresh focused TypeScript and production Vite build both pass after the E2E rewrite.
- First full E2E attempt: 20/28 passed. Eight failures are four duplicated desktop/mobile locator timeouts (voucher rows, reviewer evidence action, recycling submit label); captured artifacts will be used for targeted correction.
- Failure DOM confirmed rendered controls were present. Corrected two whitespace-sensitive voucher accessible-name regexes, one stale recycling button label, and a reviewer test override that intercepted GET queue requests instead of falling through; product behavior was not weakened.
- Focused retry passed the device-gate flow. Corrected user-facing voucher grouping to `DEMO 0001` while preserving raw QR data, and fixed reviewer mock matching to use URL pathname rather than an impossible query-bearing pathname.
- Full Playwright E2E now passes 28/28 across Chromium desktop and Pixel 7. Phase 3 implementation is complete; Phase 4 repository-wide and rendered fidelity verification is active.
- Repository-wide `pnpm typecheck`, `pnpm build`, and `pnpm test` pass. The default unit command reports 26 API tests passed and 43 database/integration-gated tests skipped by their existing environment guards; domain 23, contracts 18, worker 5, and evidence-lib 3 tests pass.
- Connected to the Codex in-app browser for local rendered QA and loaded the local-development and screenshot verification guidance before interaction.
- Confirmed the web/API processes are listening. Loaded the in-app visibility and responsive viewport controls; no existing user tab was available to claim, so QA will use one new tab and leave it as the deliverable.
- Opened a new in-app product tab successfully; the first combined inspection stopped only at an incorrect screenshot method. The existing tab remains usable and will be inspected with the tab-level screenshot API.
- Verified the in-app welcome page identity, meaningful DOM, screenshot, and zero console warnings/errors. Started the real user session; the shell rendered and Home data was still loading at the first capture, so the next check will wait on the screen heading.
- Verified the fully loaded live Home, Wallet, and used Voucher detail in the in-app browser. The current 3-point/used-voucher state is legible, consumer-safe, and free of console warnings/errors; used state correctly removes QR and merchant-show action.
- GitHub publication requested: verified this workspace is not yet a Git repository; `.env`, dependencies, build output, test output, coverage, and local session artifacts are covered by `.gitignore`. GitHub CLI authentication is active for `umyunsang`, and `umyunsang/Net-Zero` did not exist at the time of the check. Publication will preserve source, contracts, documentation, evidence artifacts, and planning records in scoped commits.
- Publication validation: the first staged workspace check found one Markdown trailing-whitespace warning in `README.md` and the initial non-content secret-scan command used an invalid option order. The README was corrected and the scan will be rerun before commits; no credential value was printed or added to the index.
- Publication hygiene: the first web commit temporarily included two generated `*.tsbuildinfo` files. Before any push, their index entries will be removed while preserving the local files, and `.gitignore` will permanently exclude TypeScript build metadata.
- GitHub publication complete: created the private repository `umyunsang/Net-Zero`, added it as `origin`, and pushed the initial `main` history. The remote head was verified against local commit `2b7bcb6697f8d09c414110d7560885a712cb4dbc`; `pnpm typecheck` passed before the push. No environment file, dependency tree, build output, test output, TypeScript build metadata, or `.gjc` session artifact was committed.
- Received live-render feedback that consumer typography remains too narrative. Added a Phase 4 refinement gate: shorten visible copy to nouns, numbers, status, and direct actions; preserve all business and verification semantics.
- Rewrote primary consumer copy around four short nouns (`คะแนน`, `กิจกรรม`, `รางวัล`, `บัตร`) and shortened Home, Activity, capture, History, Wallet, Voucher, and Profile labels without changing routes or data contracts.
- Strengthened the type hierarchy with tighter high-weight headings, tabular numeric emphasis, quieter supporting labels, and direct action styling; updated the E2E assertions to the new visible copy.
- Typography refinement static checks pass: repository-wide `pnpm typecheck` and `pnpm build` completed successfully.
- First refreshed E2E run passed 23/28. All five failures reduce to two copy-locator issues introduced by the refinement: duplicate `ทำกิจกรรม` names and one stale locked-reward assertion. The CTA is now `เริ่มกิจกรรม`, preserving brevity while restoring unique accessible intent.
- Refreshed full Playwright E2E passes 28/28 across Chromium desktop and Pixel 7 after the targeted copy-locator corrections.
- In-app browser Home reload and real-session entry pass at `http://127.0.0.1:5173/`; page identity, semantic DOM, and error/warning console checks are clean. Saved the 1440×1000 desktop Home render for fidelity review.
- Verified the 390×844 CSS mobile Home at the renderer's 2× output density and saved both the retina source and a true 390×844 PNG derivative.
- Clicked through the live mobile Activity and Wallet destinations. Their shortened copy, active navigation, real 3-point/used-voucher state, and console health all pass rendered QA.
- Captured a fresh 430-wide in-app Home render that resolves the prior half-width screenshot artifact. Added an explicit Pixel 7/desktop assertion that document scroll width never exceeds viewport width.
- Final E2E rerun passes 28/28 with the new overflow assertion across Chromium desktop and Pixel 7.
- Direct `view_image` comparison completed for accepted versus latest desktop and mobile Home. Five-plus fidelity points pass: navigation/skeleton, numeric hierarchy, activity rows, palette, container model, reward/recent placement, and responsive behavior. Copy differences are the explicit user-authorized typography refinement.
- Final repository verification passes: `pnpm typecheck`, `pnpm build`, `pnpm test`, and full web E2E. Unit results remain 26 API pass with 43 environment-gated skips, domain 23, contracts 18, worker 5, and evidence-lib 3.
- Removed invalid intermediate mobile screenshot crops; retained only valid desktop/mobile render evidence outside the repository. Phase 4 is complete.
- The first completion-check command did not resolve the slug-scoped plan from repository root. Retrying from the active plan directory before final handoff.
- Completion checker passes from the active plan directory: `ALL PHASES COMPLETE (4/4)`. Final Git whitespace check also passes.
- Reset the temporary responsive viewport. The redundant click on the already-active Home navigation item hit an in-app browser coordinate-translation issue; the page itself remains on Home and will be captured without repeating that click.
- Added Phase 5 for presentation-speed activity completion. The implementation will keep the fast assumptions demo-only and preserve the real verification boundary outside `mock_demo`.
- Confirmed the existing boundaries: bus is already oracle-verified from the complete trace; recycling alone required a reviewer after QR submission.
- Implemented a 400 ms demo bus replay interval while retaining 30-second sample timestamps, then automatically uploads and submits after the sixth interval (about 2.4 seconds from Start).
- Implemented recycling auto-verification only for mock-demo QR/account/config scope; it stores the approved count, uses the existing impact/point credit path, and leaves non-demo submissions pending review.
- Updated the consumer success screen to show verified completion and returned points, and changed the demo recycling action to one direct `ส่งรีไซเคิล` action.
- Focused API/web TypeScript checks pass. The updated desktop device-gate E2E passes and verifies recycling `+20 คะแนน`, automatic 2.4-second bus submission, preserved 30-second evidence timestamps, and background cancellation.
- Repository-wide `pnpm typecheck`, `pnpm build`, `pnpm test`, full desktop/Pixel 7 Playwright (28/28), and `git diff --check` pass. Database-conditioned API suites retain their existing skip guards; the running local API was exercised directly in the in-app browser.
- Live bus verification passed from one Start action to `สำเร็จแล้ว` in about 2.4 seconds with no second submit action. Browser logs contained only Vite debug and React DevTools info entries, with no warning or error.
- Reset isolated `mock_demo` state after discovering its one-time QR had already been consumed, then verified a 46-item recycling submission returned `+20 คะแนน` immediately with no reviewer role.
- Reset `mock_demo` once more after QA, re-entered the zero-point consumer session, and left the Codex in-app browser on the Activity hub with bus, recycling, and tree ready for presentation. Phase 5 is complete.
- Added Phase 6 for the user-approved demo reward policy, populated pseudonymous leaderboard, and immediate Thai/English/Korean product switching. Current implementation discovery is active; existing dirty redesign changes remain preserved.
- The user reattached the seven approved DS renders and identified visual drift in the implementation. Phase 6 now restores fidelity to those exact mobile/desktop compositions while integrating the newly requested scoring, leaderboard, and language behavior.
- Implemented mock-demo-only fixed rewards: verified tree claims now award 15 points and verified bus claims award 3 points. A database fallback ensures bus still receives 3 points when its calculated carbon score would otherwise round to zero; production scoring and carbon factors are unchanged.
- Added a deterministic eight-person Thai pseudonymous leaderboard to demo scope, merged with opted-in demo users, sorted/ranked by weekly points, and exposed as a Home preview plus full Profile ranking.
- Added a global, persistent Thai/English/Korean selector. Static translation coverage audit found 204 used UI keys with zero missing English or Korean entries; switching updates the visible consumer and operator UI immediately and survives reload.
- Restored the approved DS composition: desktop rail, large point hero, activity line scenes, wallet cards, mobile app header, bottom navigation, focused detail headers, white canvas, green line work, and orange reward accents.
- Applied migrations 002 and 003 to the local demo database. A fresh isolated PostGIS migration run passed 3/3, and the uniquely named temporary verification database was removed.
- Isolated full demo verification passed 10/10: tree 15 + recycling 20 + bus 3 = 38 points, populated leaderboard, voucher issue/debit, production separation, and corrected-ledger behavior.
- Final verification passed: repository `pnpm typecheck`, `pnpm build`, `pnpm test`, and web Playwright 30/30 across Chromium desktop and Pixel 7.
- In-app browser QA passed at `http://127.0.0.1:5173/`: 0-point Home, Home leaderboard preview, full eight-row Profile leaderboard, bus 3/tree 15 labels, Thai→English→Korean immediate switching, English reload persistence, and zero console warnings/errors.
- Reset only the deterministic `mock_demo` state after QA. The in-app browser is left on the Thai 0-point Home, ready for presentation. Phase 6 is complete.
- The user rejected the Phase 6 visual result as different from the approved design and explicitly required `DS@v1` fidelity plus an Open Design rebuild of the mock leaderboard. Reopened work as Phase 7; earlier functional verification remains evidence, but the earlier visual sign-off is superseded.
- Reverified the exact `DS@v1` SHA-256 and all seven approved render paths before making new product changes.
- Confirmed the Open Design brief as cross-platform, one core flow, and polished product UI. The default Open Design runtime failed before generation because its sign-in had expired; no artifact was created. Restarted the identical brief with the installed Local Codex runtime, which is currently generating the leaderboard prototype.
- Began bounded fidelity correction while Open Design runs: restored the approved DS color tokens, 248 px desktop rail, 1180 px content cap, 300–340 px secondary column, approved Home labels, mobile balance/illustration composition, and eight populated Thai-safe mock leaderboard identities. Focused web typecheck passes.
- The user elevated typography to an explicit acceptance surface. Phase 7 verification now covers Thai/English/Korean font fallback, weight, line height, wrapping, title width, numeric hierarchy, and legibility at both mobile and desktop breakpoints.
- Completed the requested functional bundle in this session: tree 15 points, bus 3 points, deterministic eight-person Thai mock leaderboard, and immediate persistent Thai/English/Korean switching. Fresh isolated demo verification passes 10/10 and full desktop/Pixel 7 browser verification passes 30/30.
- Replaced the translated header wordmark and PWA/browser app name with the invariant project name `Net Zero`; focused language tests and live Thai/English/Korean browser inspection confirm the brand never changes while the product copy does.
- Reset only deterministic `mock_demo` data to 0 points, restored the default browser viewport, verified zero browser warnings/errors, and left the Codex in-app browser open on the Thai Home screen.
- The user explicitly identified the seven original generated images as the approved design. Verified dimensions are five mobile originals at 853×1844 and two desktop originals at 1586×992. Locked SHA-256 values, in the user-supplied order, are `a03db0ff9f842e5dcd27e51767fb4d4c05a478bcba51df44dc9b1fb444008aea`, `20308522aa17a5284124e9a2a1ff61f40ebb0330182c35537333707d32062acf`, `8ad1de39fd1c14a988aba9f5052004a30c9caef97081f24b5d93f4a882ba7cba`, `fa6ba10b62814134ecc04f20b3dcb74d72620731cdd87dfe93b76013a641770d`, `ba115b49cec530ae805387bf276b928319ec6c677ea3e489a1237c90e283e696`, `b3d9b867e81698f4643ee519c211cc537f0594dbf1049dad4c4668ee4e07b804`, and `be4c6807fa8849e3f22e3c1c7e01b018a6c82f8a233031c6722ec368bff4b2be`.
- Completed the second Open Design refinement against all seven originals. The final standalone leaderboard artifact preserves the DS@v1 shell and interaction flow while normalizing language-aware TH/EN/KO stacks, weights, line boxes, number alignment, name truncation, and 390/430/1024/1440 layout contracts. Its SHA-256 is `8fd7e4718bc764fe98a962af64473f3a8b3d9f31e4efae4ea7bc364ca668da66`.
- Open Design static and interaction checks passed. Its single rendered export attempt was unavailable because the local desktop renderer socket did not exist; this is retained as an explicit evidence limit and will be replaced by direct in-app product browser inspection rather than claimed as a visual pass.
- Integrated the Open Design interaction model as a separate weekly leaderboard screen. Home and Profile link into it without a fifth navigation destination; the full list retains the community rows after opt-out, highlights the viewer while joined, and uses an explicit confirmation step.
- Replaced the old embedded Profile consent tests with separate-screen tests. Focused leaderboard coverage passes 6/6, and the complete desktop/Pixel 7 Playwright suite passes 30/30.
- Completed the language/static type audit: 209 used translation keys have zero missing English or Korean values; no `550/650`, `overflow-wrap:anywhere`, or `word-break:break-all` remains in product UI.
- Applied language-aware local font variables, Thai 1.6 body leading, exact 16/24 activity and reward row titles, 44 px language controls, tabular rank/point numerals, and Thai negative-tracking guards. Preserved the newer invariant `Net Zero` shell wordmark while retaining every other approved DS@v1 visual detail.
- Re-read the shared workspace after the source-session handoff and corrected the transient wordmark conflict: `Net Zero` is again invariant in `BrandMark`, browser/PWA metadata, and the TH→EN→KO→reload E2E contract.
- Direct product-browser QA passes at 390, 430, and 1440 px. Mobile Home aligns the approved 72 px shell, 26/32.5 title, 48/48 point numeral, 16/24 rows, eco-city motif, and four-item bottom navigation; desktop confirms the 248 px rail, 1180 px content cap, 340 px Home secondary column, and 320 px leaderboard participation card. No inspected surface has horizontal overflow.
- The integrated leaderboard shows eight deterministic Thai community rows plus the joined viewer at 0 points, preserves the four primary destinations, and remains usable in Thai, English, and Korean with the intended local font stack and wrapping behavior.
- Final Phase 7 verification passes: repository typecheck/build/test, fresh-database migrations 3/3, demo separation/full flow 10/10, Playwright desktop + Pixel 7 30/30, and `git diff --check`. The first DB rerun exposed a stale migration state; applying 002/003 restored the verified tree 15 + recycling 20 + bus 3 = 38 contract.
- Reset only deterministic `mock_demo` data after QA. Confirmed balance 0, demo claims 0, demo vouchers 0, production claims 0, and left the in-app product on the Thai 0-point Home. Phase 7 is complete.
- Reopened the visual gate as Phase 8 after the user supplied a Korean desktop Wallet screenshot with collapsed reward text. Reproduced at 1024×900: each 306 px reward card was forced into `78px 0px 150px`, leaving its copy column at exactly 0 px and causing syllable-by-syllable wrapping.
- Corrected the responsive reward-card grid: 1024–1279 px now uses a width-safe icon/body layout, while the three-column action layout activates only from 1280 px and only for available rewards. Korean reward titles, points, and remaining-point copy use `word-break: keep-all`.
- Rendered verification passes at 390, 1024, and 1440 px. At the failing 1024 px boundary the first card is now 164 px tall with a 142 px copy track (`78px 142px`), compared with the broken 386 px card and `78px 0px 150px`; horizontal overflow remains zero and browser warning/error logs are empty.
- Added an exact 1024 px regression assertion for a nonzero copy track, minimum 120 px readable copy width, and at-most-two-line Korean reward title. Focused desktop/Pixel tests pass 2/2; web typecheck/build and the full desktop/Pixel suite pass 30/30. Phase 8 is complete.
