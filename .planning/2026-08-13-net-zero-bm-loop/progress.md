# Progress: Net-Zero earn-to-voucher BM loop

## 2026-08-13
- Routed the request as a material product/BM change rather than a simple rendering check.
- Read the mandatory preprocessing workflow and started a separate scoped plan.
- Recorded the raw request and normalized the intake without treating inferred scoring or current-flow completeness as fact.
- No product code, runtime data, or external service has been changed.
- Read the canonical README, architecture invariants, product-claims language, prior-memory navigation hints, and candidate approval/spec locations.
- Locked the non-negotiable boundaries: verified-only credit, atomic point ledger/balance, atomic voucher debit/issue, one-time redemption, Thai-only demo-safe claims, and no production/payment/carbon-credit representation.
- Read the complete deep-interview specification and approved stage-05 plan/receipt. Reconciled the current request to the already approved end-to-end demo acceptance criteria and recorded implementation authorization.
- Audited the current React flow, demo seed, point calculation, dashboard read model, voucher lifecycle, and full-demo integration evidence.
- Confirmed the real gap: the core loop exists but is not narrated as one live business journey, reward affordability is not shown, and the persisted voucher-list response does not match the frontend shape needed to show post-redemption state.
- Chose the smallest approved delta: live dashboard checkpoints, dynamic reward affordability/debit handoff, merchant redemption receipt, one normalized voucher representation, and focused regression tests. Locked scoring and verification semantics remain unchanged.
- Implemented the first pass and passed web/API typechecks plus the real full-demo integration test. The initial desktop E2E run had two locator-only strictness failures caused by intentionally repeated visible copy; product behavior passed. Tightened both locators to their named regions before rerun.
- User rejected the presentation/deck-like visual direction and exposure of internal requirements to end users. This plan is paused; its functional BM-loop changes remain current-state evidence only, while all presentation-facing layout and copy are superseded pending a new consumer-product skeleton and design approval.
